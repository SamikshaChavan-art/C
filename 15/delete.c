#include<stdio.h>
// Delete a key element in array
int main(){
    int a[100], n, i, j, key;
    prinf("Enter N:");
    scanf("%d", &n);
    printf("Enter N Element:");
    for ( i = 0; i < n; i++)
        scanf("%d", &a[i]);
    

    
}